# Rust
/target/

# Node / Svelte
node_modules/
/build/
/.svelte-kit/
/package/

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Local env
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db

# Local cargo path overrides (e.g. local [patch] entries). The committed
# .cargo/config.toml is for project-wide linker flags and stays tracked.
.cargo/config.toml.local
src-tauri/.cargo/

# Claude Code memory (local-only)
.claude/
