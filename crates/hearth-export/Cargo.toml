[package]
name = "hearth-export"
version.workspace = true
authors.workspace = true
license.workspace = true
edition.workspace = true
repository.workspace = true
description = "Serde schema for Hearth's owned-blueprints JSON. Consumed by sc-langpatch."

[dependencies]
# Serde-only by design — no sc-holotable, so sc-langpatch can consume this
# without inheriting Hearth's sc-holotable version. Guids cross as hex strings.
serde.workspace = true
